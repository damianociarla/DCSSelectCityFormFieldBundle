{% block select_city_widget %}
{% spaceless %}
    {{ form_widget(form) }}
    <script type="text/javascript">
    window.onload = function () {
        window.select_city_instance.push(new window.select_city('{{ form.country.vars.id }}', '{{ form.state.vars.id }}', '{{ form.city.vars.id }}'));
    }
    </script>
{% endspaceless %}
{% endblock %}